[pytest]
# Make the repo root importable so tests can `import build_lib` and
# `from checks.leak_gate import ...` without relying on implicit namespace
# packaging or the location pytest happens to be invoked from.
pythonpath = .
